{
  "name": "edge-tutor",
  "version": "0.10.5",
  "description": "认知边缘导师：选中教材文本追问 AI（价值识别器，绝不拉回）+ 执行模式 agent（委托 opencode 全能力执行）。沉淀认知节点笔记 + 原理链认知地图",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs && node scripts/copy-wasm.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "test": "node scripts/build-tests.mjs && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@huggingface/transformers": "^4.2.0",
    "esbuild": "^0.28.2",
    "obsidian": "^1.7.2",
    "typescript": "^5.9.3"
  }
}
