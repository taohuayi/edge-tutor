{
  "name": "edge-tutor",
  "version": "0.11.0",
  "description": "An Obsidian plugin for branching AI learning conversations, source anchors, and durable knowledge maps.",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs && node scripts/copy-wasm.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "test": "node scripts/build-tests.mjs && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@huggingface/transformers": "^4.2.0",
    "esbuild": "^0.28.2",
    "obsidian": "^1.7.2",
    "typescript": "^5.9.3"
  }
}
